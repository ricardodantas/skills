---
"skills": minor
---

Add the `hugo-write-post` skill — in a Hugo repo, it learns the author's writing style from their existing posts and writes a new post on a given topic that matches that voice, placing it with correct Hugo front matter. Delegates the prose to the `social-content` skill.
